apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "world-trade-map.fullname" . }}-test-connection"
  labels:
    {{- include "world-trade-map.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "world-trade-map.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "world-trade-map.fullname" . }}
  labels:
    {{- include "world-trade-map.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "world-trade-map.selectorLabels" . | nindent 4 }}
